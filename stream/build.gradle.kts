// :stream — placeholder. Establishes the module boundary from
// ARCHITECTURE.md now; StreamResolver and its implementations land in a
// later stage. Must never depend on :sync:youtube — see ARCHITECTURE.md
// constraint 3.
