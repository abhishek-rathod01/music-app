// :sync:youtube — placeholder. Establishes the module boundary from
// ARCHITECTURE.md now; the YouTube Data API v3 OAuth sync lands in a later
// stage. Must never depend on :stream — see ARCHITECTURE.md constraint 3.
